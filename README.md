# representational_analysis

Reserved for analysis of the models' internal representations across layers
(e.g. last-token hidden states `h_t^(L)`, layer-wise probing of content vs.
positional bias). Scope to be defined — nothing implemented here yet.
